import SwiftUI

enum BiologerColors {
    // Brand palette
    static let brandAccent = Color(uiColor: BiologerUIColor.brandAccent)
    static let brandStrong = Color(uiColor: BiologerUIColor.brandStrong)
    static let brandSoft = Color(uiColor: BiologerUIColor.brandSoft)
    static let brandCanvas = Color(uiColor: BiologerUIColor.brandCanvas)
    static let brandInk = Color(uiColor: BiologerUIColor.brandInk)

    // Semantic roles
    static let accent = Color(uiColor: BiologerUIColor.accent)
    static let primaryActionBackground = Color(
        uiColor: BiologerUIColor.primaryActionBackground
    )
    static let primaryActionForeground = Color(
        uiColor: BiologerUIColor.primaryActionForeground
    )
    static let destructive = Color(uiColor: BiologerUIColor.destructive)
    static let pageBackground = Color(uiColor: BiologerUIColor.pageBackground)
    static let surface = Color(uiColor: BiologerUIColor.surface)
    static let textPrimary = Color(uiColor: BiologerUIColor.textPrimary)
    static let sectionTitle = Color(uiColor: BiologerUIColor.sectionTitle)
    static let iconBackground = Color(uiColor: BiologerUIColor.iconBackground)
    static let selectedSurface = Color(uiColor: BiologerUIColor.selectedSurface)
}
